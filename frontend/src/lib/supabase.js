/**
 * supabase.js — Authentication removed.
 *
 * The Supabase auth client is no longer used. This stub is kept so
 * that any stale imports of `supabase` don't cause build errors.
 */
export const supabase = null;
